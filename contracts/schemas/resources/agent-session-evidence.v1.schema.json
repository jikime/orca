{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://schemas.pielab.ai/resources/agent-session-evidence.v1.schema.json",
  "title": "Agent session evidence page",
  "description": "A scoped, optionally-searched page of a session's Evidence (R5 s5a). scope is the effective visibility scope the read was resolved to; items above that scope are absent, and a search term never matches a redacted record — proving internal prompts and restricted tool output do not surface in customer evidence or search results.",
  "type": "object",
  "required": ["session", "scope", "items", "nextCursor"],
  "properties": {
    "session": { "$ref": "https://schemas.pielab.ai/resources/agent-session.v1.schema.json" },
    "scope": { "enum": ["internal", "project", "customer"] },
    "items": {
      "type": "array",
      "items": { "$ref": "https://schemas.pielab.ai/resources/agent-evidence-item.v1.schema.json" }
    },
    "nextCursor": {
      "oneOf": [{ "type": "string", "minLength": 1, "maxLength": 512 }, { "type": "null" }]
    }
  },
  "additionalProperties": false
}
