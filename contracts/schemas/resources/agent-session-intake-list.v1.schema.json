{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://schemas.pielab.ai/resources/agent-session-intake-list.v1.schema.json",
  "title": "Agent session intake page",
  "description": "A cursor-paged page of the unassigned-session intake queue (doc 19 :162, R5 s4b), newest-first. Defaults to pending items; filterable by capture scope (host / workspace / provider / source type).",
  "type": "object",
  "required": ["items", "nextCursor"],
  "additionalProperties": false,
  "properties": {
    "items": {
      "type": "array",
      "items": { "$ref": "https://schemas.pielab.ai/resources/agent-session-intake.v1.schema.json" }
    },
    "nextCursor": {
      "oneOf": [{ "type": "string", "minLength": 1, "maxLength": 512 }, { "type": "null" }]
    }
  }
}
