[project]
name = "pie-meeting-transcription-agent"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
  "livekit-agents[openai,silero]~=1.5.0",
]

[tool.ruff]
line-length = 100
target-version = "py311"
